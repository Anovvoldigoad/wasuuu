name: Build Winlator Edition

on:
  workflow_dispatch:
  push:
    branches: [ main, winlator-port ]

jobs:
  build:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.424'

      - name: Show SDK
        run: dotnet --info

      - name: Restore
        run: dotnet restore NSC-ModManager.Winlator.csproj

      - name: Publish win-x86 self-contained
        run: dotnet publish NSC-ModManager.Winlator.csproj -c Release -r win-x86 --self-contained true --no-restore -o publish/win-x86

      - name: Preflight published files
        shell: cmd
        run: check-publish.bat publish\win-x86

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: NSC-ModManager-Winlator-win-x86
          path: publish/win-x86/**
